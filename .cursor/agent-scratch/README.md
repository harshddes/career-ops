# Agent scratch (do not use the repo root)

Parallel CLI dumps, web-search JSON, and other agent research files belong here:

```text
.cursor/agent-scratch/{filename}
```

Not in the project root. Not next to `package.json`.

This folder is gitignored except this README. Create it if missing.
